package victor.training.performance;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.client.RestTemplate;

import java.util.LinkedHashMap;
import java.util.UUID;

public class GCIntro {
//  @Autowired
//  private  RestTemplate rest;
//private static final Map<> cacheWith$hit = new LinkedHashMap();
//  @GetMapping
//  public void get(UUID id) {
//    var data = repo.find(id)
//    cacheWith$hit.put(id, data)
//    stuff();
//    restTemplate.get(call 1sec
//    cacheWith$hit.remove(id);
//    return data+dataFroMRest
//  }

  private void stuff() {
    throw new RuntimeException("Method not implemented");
  }
}
